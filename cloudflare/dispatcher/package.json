{
  "name": "nse-pipeline-dispatcher",
  "private": true,
  "type": "module",
  "description": "Cloudflare Cron -> GitHub workflow_dispatch. Triggers only; holds no data.",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev:scheduled": "wrangler dev --test-scheduled",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "4.139.0"
  }
}
